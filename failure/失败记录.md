## attention-guided split 失败记录（2026-06-10）
现象：attention 分数高的样本并不代表对 F 分布更有代表性
原因推测：QASSMax 在架构层已 query-aware，attention 本身是 query-conditioned 的，
          用它来判断"样本与 query 的相关性"是用一个已被偏置的信号做二次选择
结论：停止此方向，改用特征空间分布距离
影响：QACR 的选择信号从 attention 改为 MMD/均值距离


